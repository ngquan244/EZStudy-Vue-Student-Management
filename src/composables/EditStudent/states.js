// states.js
import { ref } from 'vue'

export const student = ref(null)
export const name = ref('')
export const birthDate = ref('')
export const selectedClass = ref('')
export const classList = ref([])
